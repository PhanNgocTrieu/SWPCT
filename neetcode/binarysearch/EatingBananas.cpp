#include <bits/stdc++.h>
using namespace std;
class Solution {
public:
    int minEatingSpeed(vector<int>& piles, int h) {
        
    }
};

int main() {
    std::ios::sync_with_stdio(0);
    std::cin.tie(0);
    std::cout.tie(0);

    // vector<vector<int>> matrix = {
    //     {1,3,5,7},
    //     {10,11,16,20},
    //     {23,30,34,60}
    // };

    vector<int> matrix = {
        1,4,3,2
    };

    Solution{}.minEatingSpeed(matrix, 10);

    return false;
}